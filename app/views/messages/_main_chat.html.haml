.chat-main
  .main-header
    .main-header__leftbox
      .main-header__leftbox__current-group
        = @group.name
        -#= group???
      .main-header__leftbox__member-list
        Member： 
        .main-header__leftbox__member-list__member
          - @group.users.each do |user|
            = user.name
    = link_to edit_group_path(@group.id), class: "main-header__edit-btn" do
      Edit
  .messages
    = render @messages
      

        
  -# -# .form
  -# -#   = new_message [@group, @message] do |f|
  -# -#     =f.input-box :content, class: 'form__message', placeholder: 'type a message'
  -# -#       %input.input-box__text{type: "text" , placeholder: "type a message" }
  -# -#       %label{class: "input-box__image"}
  -# -#         = icon('far','image', class: 'icon')
  -# -#         %input{type: "file", class: "input-box__image__file"}
  -# -#     = %input{type: "submit", class: "submit-btn",value: "Send"}

  .form
    = form_for [@group, @message], html:{class:"new_message"}, local:true do |f|
      .input-box
        = f.text_field :content, class: 'input-box__text', placeholder: 'type a message'
        = f.label :image, class: 'input-box__image' do
          = icon('fas', 'image', class: 'icon')
          = f.file_field :image, class: 'input-box__image__file'
      = f.submit 'Send', class: 'submit-btn'

-# %= form_with(model: [@tweet, @comment], local: true, id: "new_comment") do |form| %> id:   id: "new_comment"
-#   <%= form.text_area :text, placeholder: "コメントする" , rows: "2", class: "textbox" %>     class: "textbox"
-#   <%= form.submit "SEND", class: "form__submit" %>                                         class: "form__submit
-# <% end %>


-# form
-#   new_message
-#     input-box
-#       input-box__text
-#       input-box__image
-#         アイコンボタン
-#         input-box__image__file
-#     submit-btn